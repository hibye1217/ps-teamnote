Given a DAG, Minimum Vertex Path Cover can be computed as follow.
Divide the vertex $v$ onto $v_l$ and $v_r$. For each edge $v \rightarrow w$, connect $v_l$ and $w_r$.
Number of path you need to use is the $V$ - Maximum Matching.

Minimum Edge Path Cover is just Eulerian Path in disguise.