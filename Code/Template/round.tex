\begin{minted}{cpp}
inline ll ffloor(ll a, ll b){
	if (b < 0){ a = -a; b = -b; }
	return (a >= 0 ? a/b : (-a+b-1)/b*-1);
}
inline ll fceil(ll a, ll b){
	if (b < 0){ a = -a; b = -b; }
	return (a >= 0 ? (a+b-1)/b : -a/b*-1);
}
\end{minted}